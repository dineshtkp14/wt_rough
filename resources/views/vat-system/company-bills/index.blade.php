@extends('layouts.master')
@section('content')
<div class="main-content" style="background:#eef3f9;min-height:100vh"><div class="container-fluid p-4"><div class="d-flex justify-content-between mb-4"><div><h1 class="fw-bold">Company Purchase Bills</h1><p class="text-muted">Bills received from companies and suppliers.</p></div><a href="{{ route('vat-system.company-bills.create') }}" class="btn btn-primary">+ Add Company Bill</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead class="table-primary"><tr><th>Bill No.</th><th>Date</th><th>Company</th><th>VAT/PAN</th><th class="text-end">Grand Total (VAT Included)</th><th>Actions</th></tr></thead><tbody>@forelse($bills as $bill)@php $total=$bill->items->sum(fn($i)=>(float)$i->quantity*(float)$i->rate); $taxable=$bill->items->where('is_taxable',true)->sum(fn($i)=>(float)$i->quantity*(float)$i->rate); $vat=round($taxable*.13,2); $grand=$total+$vat; @endphp<tr><td><strong>#{{ $bill->bill_no }}</strong></td><td>{{ $bill->bill_date->format('Y-m-d') }}</td><td>{{ $bill->company_name }}</td><td>{{ $bill->company_vat_no ?: ($bill->company_pan_no ?: '-') }}</td><td class="text-end fw-bold">{{ number_format($grand,2) }}</td><td><a href="{{ route('vat-system.company-bills.show',$bill) }}" class="btn btn-info btn-sm text-white">View</a> <a href="{{ route('vat-system.company-bills.edit',$bill) }}" class="btn btn-warning btn-sm">Edit</a><form class="d-inline" method="post" action="{{ route('vat-system.company-bills.destroy',$bill) }}" onsubmit="return confirm('Delete this company bill?')">@csrf @method('DELETE')<button class="btn btn-danger btn-sm">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="text-center p-5">No company purchase bills saved.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $bills->links() }}</div></div></div></div>
@endsection


