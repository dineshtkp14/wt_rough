@extends('layouts.master')
@section('content')
<div class="main-content" style="background:#eef4fb;min-height:calc(100vh - 70px);padding:28px"><div class="container" style="max-width:900px"><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="fw-bold text-primary">{{ $supplier ? 'Edit Supplier' : 'Add Supplier' }}</h1><p class="text-muted mb-0">{{ $firm->name }}</p></div><a href="{{ route('vat-system.suppliers.index') }}" class="btn btn-outline-secondary">Back</a></div><div class="card border-0 shadow-sm"><div class="card-header bg-primary text-white fw-bold">Supplier Details</div><div class="card-body p-4"><form method="post" action="{{ $supplier ? route('vat-system.suppliers.update',$supplier) : route('vat-system.suppliers.store') }}">@csrf @if($supplier) @method('PUT') @endif<div class="row g-3"><div class="col-md-6"><label class="form-label fw-bold">Supplier Name *</label><input name="name" class="form-control" value="{{ old('name',$supplier->name ?? '') }}" required></div><div class="col-md-3"><label class="form-label fw-bold">VAT No.</label><input name="vat_no" class="form-control" value="{{ old('vat_no',$supplier->vat_no ?? '') }}"></div><div class="col-md-3"><label class="form-label fw-bold">PAN No.</label><input name="pan_no" class="form-control" value="{{ old('pan_no',$supplier->pan_no ?? '') }}"></div><div class="col-md-5"><label class="form-label fw-bold">Phone</label><input name="phone" class="form-control" value="{{ old('phone',$supplier->phone ?? '') }}"></div><div class="col-md-7"><label class="form-label fw-bold">Address</label><input name="address" class="form-control" value="{{ old('address',$supplier->address ?? '') }}"></div></div><button class="btn btn-primary mt-4"><i class="fa fa-save me-1"></i>{{ $supplier ? 'Update Supplier' : 'Save Supplier' }}</button></form></div></div></div></div>
@endsection
