@extends('layouts.master')
@section('content')
<div class="main-content vat-stock-page"><div class="container-fluid p-3 p-md-4"><div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-4"><div><h1 class="fw-bold mb-1"><i class="fa fa-clock-rotate-left me-2"></i>Stock History</h1><p class="text-muted mb-1">{{ $stock->item_name }} · {{ $stock->unit }}</p><span class="badge text-bg-primary">Every IN and OUT movement</span></div><a href="{{ route('vat-system.stock.index') }}" class="btn btn-outline-secondary"><i class="fa fa-arrow-left me-1"></i>Back to Stock</a></div><div class="stock-card overflow-hidden"><div class="p-3 border-bottom"><h4 class="mb-1 fw-bold">Movement Records</h4><small class="text-muted">Who changed the stock, when, and from what quantity to what quantity.</small></div><div class="table-responsive"><table class="table table-hover mb-0 align-middle"><thead><tr><th>Date &amp; Time</th><th>Type</th><th>Quantity</th><th>Before</th><th>After</th><th>Rate</th><th>User</th><th>Reference / Notes</th></tr></thead><tbody>@forelse($movements as $movement)<tr><td>{{ $movement->created_at->format('Y-m-d H:i') }}</td><td><span class="movement-badge {{ str_contains($movement->movement_type,'out')||str_contains($movement->movement_type,'sale')?'movement-out':'movement-in' }}">{{ strtoupper(str_replace('_',' ',$movement->movement_type)) }}</span></td><td class="fw-bold">{{ number_format($movement->quantity,3) }}</td><td>{{ $movement->quantity_before !== null ? number_format($movement->quantity_before,3) : '-' }}</td><td>{{ $movement->quantity_after !== null ? number_format($movement->quantity_after,3) : '-' }}</td><td>Rs {{ number_format($movement->rate,2) }}</td><td>{{ $movement->user_email ?: '-' }}</td><td>{{ $movement->reference ?: '-' }}@if($movement->notes)<br><small class="text-muted">{{ $movement->notes }}</small>@endif</td></tr>@empty<tr><td colspan="8" class="text-center p-5">No stock movements recorded yet.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $movements->links() }}</div></div></div></div>
<style>.movement-badge{display:inline-block;padding:6px 9px;border-radius:20px;font-size:11px;font-weight:800}.movement-in{background:#dcfce7;color:#15803d}.movement-out{background:#fee2e2;color:#b91c1c}</style>
@endsection
